[package]
name = "sandbox"
version = "0.1.0"
edition = "2024"

[dependencies]
chronographer = { path = "../core", features = ["macros"] }
tokio = { version = "1.50.0", features = ["full"] }
async-trait = "0.1.89"
